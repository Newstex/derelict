{
  "name": "derelict",
  "version": "0.1.0",
  "description": "DERELICT — A sci-fi space station survival RPG. Solo, offline, browser-based.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/three": "^0.165.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
